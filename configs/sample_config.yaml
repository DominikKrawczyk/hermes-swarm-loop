name: "Common Hermes Swarm Run"
description: "Standard 33-agent build cycle"

# Phase to run (prd_build | development | quality | hunting | simplicity)
phase: development

# YOLO zone (safe | test | staging | production)
yolo_zone: test

# Max parallel agents (auto-capped by YOLO zone)
max_agents: 33

# Project info
project:
  name: "MyProject"
  description: "Build something great"
  repo: "DominikKrawczyk/hermes-swarm-loop"

# Skill to load
skill: hermes-swarm-loop

# Auto push to GitHub after masterpiece
auto_github_push: false
